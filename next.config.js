/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  images: {
    domains: ["fakestoreapi.com", "themoviedb.org", "images.pexels.com"],
  },
};

module.exports = nextConfig;
